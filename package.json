{
  "name": "training-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Futbol Training Lab — Train Smarter. Play Faster.",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "node scripts/serve.mjs",
    "dev:frontend:ps": "powershell -ExecutionPolicy Bypass -File ./serve.ps1",
    "dev:backend": "npm run dev --prefix backend",
    "setup": "npm run setup --prefix backend",
    "start": "npm run start --prefix backend",
    "stripe:setup": "npm run stripe:setup --prefix backend",
    "stripe:webhooks": "npm run stripe:webhooks --prefix backend",
    "db:studio": "npm run db:studio --prefix backend",
    "setup:git": "powershell -ExecutionPolicy Bypass -File ./scripts/setup-git.ps1",
    "setup:webhooks": "powershell -ExecutionPolicy Bypass -File ./scripts/setup-webhooks-local.ps1",
    "deploy:docker": "powershell -ExecutionPolicy Bypass -File ./scripts/deploy-docker.ps1",
    "build:www": "node scripts/build-www.mjs && node scripts/bundle-mobile.mjs",
    "build:www:ps": "powershell -ExecutionPolicy Bypass -File ./scripts/build-www.ps1 && node scripts/bundle-mobile.mjs",
    "cap:sync": "npm run build:www && npx cap sync",
    "assets:generate": "capacitor-assets generate --iconBackgroundColor #0f172a --iconBackgroundColorDark #0f172a --splashBackgroundColor #0f172a --splashBackgroundColorDark #0f172a",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "cap:run:android": "npx cap run android",
    "cap:run:ios": "npx cap run ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/app": "^7.0.1",
    "@capacitor/browser": "^7.0.1",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0",
    "@capacitor/splash-screen": "^7.0.1",
    "@capacitor/status-bar": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.2.0",
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.0"
  }
}
